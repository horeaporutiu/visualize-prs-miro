name: Miro PR Visualizer
on:
  pull_request:
    types: [opened, synchronize]

permissions:
  pull-requests: write

jobs:
  visualize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate Miro Architecture Diagram
        id: miro
        env:
          MIRO_API_TOKEN: ${{ secrets.MIRO_API_TOKEN }}
          BOARD_NAME: "PR Review: ${{ github.event.pull_request.title }}"
          GITHUB_URL: ${{ github.event.pull_request.html_url }}
        run: node scripts/visualize.mjs

      - name: Post Board Link to PR
        uses: mshick/add-pr-comment@v2
        with:
          message: |
            ### 🏗 Interactive Architectural Review
            A living diagram for this PR has been generated on Miro.
            [View Interactive Architectural Impact on Miro](${{ steps.miro.outputs.board_url }})
